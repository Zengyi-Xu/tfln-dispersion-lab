# 论文零号稿（草稿 v0.1）

> 依据《站位提升指南》§3-① 撰写：不写调研，先写论证。三段话 + 三个目标数。
> 日期：2026-09-25。所有引用数字均已过 verify/ 双机核查（见 `results/verify/SUMMARY.md`）。
> 用途：发给最能怼你的人挑逻辑漏洞；之后每读一篇文献，要么在支撑这页纸，要么在威胁这页纸。

---

## a) 电子方案在哪个点上结构性做不起

毫米波 / LiDAR 回波的信息**本来就写在到达时间上**（chirp 的时延-频率映射、多径的时延结构）。电子方案要识别这种 ns 级时间模式，必须先以 ≥2× 带宽采样把整个时间轴数字化——信息只有几个自由度（时延、 chirp 率、幅度），却要付出覆盖整个时间-带宽积的 ADC 采样率、存储和 DSP 功耗。这是**在检测之后才试图重建时间结构**的路线：ADC 的 ENOB-采样率-功耗三者互锁，时间分辨率每提高一倍，功耗和延迟结构性上涨，与算法优化无关。

（待补：用反向成本核算框架给出具体数字——对 T_chirp ≈ ___ ns、B = ___ GHz 的波形，ADC + 数字匹配滤波的 pJ/决策 ≈ ___，与本方案目标差 ___ 个数量级。）

## b) 什么物理过程免费给出这个算子

啁啾色散器件在**传播过程中**对时间轴做拉伸/压缩/重排——匹配滤波所需的时间反演与压缩是它的本征物理过程，不消耗 ADC、不产生数字延迟。已验证的器件侧事实（双机核查）：

- 延迟摆幅随长度线性：Δτ = 2n_gL/c，L≥5 mm 偏差 ≤3%（C2 修正后口径）；
- TFLN 延迟-损耗优值 424 ps/dB（保守 α=3.3 dB/cm），当代刻蚀 0.2–0.4 dB/cm 时升至 3.5–5.3×10³ ps/dB（C1）；
- 切趾后群延迟 ripple 2.85 → 0.09 ps（C4）；热漂移仅 0.034 ps/K（C6）——ps 级时间精度在工程上可达。

SNN 则在算法侧把 spike timing 本身当数据：色散变换的是时间，SNN 计算的也是时间。两者是同一个本体论（**时间是信息**）的两半——这是电子 ADC+DSP 路线在原理上无法复制的分工。

## c) 为什么组合产生涌现性质而非简单叠加

单独看：色散器件只是模拟前端，SNN 只是一种低功耗网络。组合后出现单独任一方都没有的性质：

1. **分辨率涌现**：分类精度受限于色散链的时间分辨（ps 级 ripple），而不是受限于任何采样率——时间分辨率与数字功耗解耦；
2. **量化鲁棒涌现**：色散展开后的时间模式对读出精度极不敏感，2–8 bit 量化全程无损（C13，10 种子），意味着读出端可以用最简电路；
3. **数据效率涌现**：在真实 KITTI 上，扫描链（色散前端 + 简单分类器）对 raw+MLP 的增益在小预算下最大（1200 样本 +0.12 ~ +0.15，随预算严格单调收窄，C15）——物理先验替代了数据，这是"前端白拿算子"的直接可测证据。

可证伪表述：**当色散链的群延迟摆幅覆盖目标波形的时间窗口、且 ripple 低于 spike 时间分辨时，时间模式分类的能效（fJ/决策）随时间分辨率提升而保持不变；电子路线则按采样定理随分辨率线性（或更差）上涨。**

---

## 三个目标数（今天定的初值，允许以后改）

| # | 指标 | 目标值 | 现有证据链 | 缺口 |
|---|---|---|---|---|
| 1 | 单次推理能耗 | < 100 fJ/决策（读出+SNN，不含激光） | 2–8 bit 无损读出（C13）；SNN 事件驱动 | 缺能耗实测/估算模型 |
| 2 | 时间模式识别分辨率 | ≤ 1 ps（等效） | ripple 0.09 ps（C4）、热漂 0.034 ps/K（C6） | 缺"ripple→分类精度"传递函数 |
| 3 | 可调色散范围 | 0.05–1.6 ps/nm（器件级），延迟摆幅 ≥ 100 ps | D=1.22–1.30 ps/nm 复现（C3）；Yu et al. Nature 2022: 1.6 ps/nm in 2.5 mm | "可调"机制（热/电光）扫描范围未定 |

## 删部件压力测试（第一轮，基于已读文献）

| 替换测试 | 故事死不死？ | 初步判断 |
|---|---|---|
| SNN → 储备池 | 待定 | 储备池也需要时间编码读出；SNN 的不可替代性 = spike timing 与色散延迟的同构，需论证 |
| 色散 → 延迟线阵列 | 故事死 | 延迟线阵列给离散抽头，给不出连续时间变换 + 匹配滤波；色散是核心 |
| TFLN → SiN | 故事半死 | SiN 延迟-损耗优值更高，但缺电光可调性；TFLN 的不可替代性完全押在"可调"上 → **目标数 3 的"可调范围"是论文生死线** |

初步结论：主角是**时间编码本身**；色散是核心器件（换不掉），TFLN 仅当"可调"被论证为必需时才不可替代——这是下一步调研最该锁死的自由度。

---

## 已知威胁清单（读文献时要找的"反方"）

1. 光纤色散 + 光 SNN 已有工作是否已占据"时间变换 + 时间计算"叙事？（占据-开放图）
2. 电子 ADC 在 ≤1 GHz 带宽下功耗其实很低——我们的目标波形带宽必须落在电子结构性做不起的区间，否则论证 a) 不成立；
3. "可调"是否真有应用需求，还是为了 TFLN 而可调？（审稿人必问）
