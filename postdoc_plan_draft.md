# 博士后研究计划（草稿）：面向光计算的 TFLN 可调色散器件与延迟敏感蓄水池

> 状态：草稿 v0（基于 2026-09-20 全部仿真结果）
> 依托平台：KAUST Yating Wan 课题组（QD 激光器 / MRR crossbar / 300 mm 异质集成）
> 外部合作：Cheng Wang 组（CityU，TFLN 加工）

---

## 摘要

本计划提出一条"快光处理 + 慢电读出"的片上 LiDAR/雷达接收新架构：
啁啾光栅在光域完成匹配滤波（脉冲压缩），输出的 ps 级光脉冲串直接进入光子蓄水池（SNN），
由慢光电积分读出完成分类。我们用仿真证明了该架构的可行性、定位了它的物理约束，
并把 TFLN 电光可调延迟从"一个器件特性"提炼为一个可研究的科学问题。

与既有"Laser-as-ReLU"计划（QD 激光器 P-I 非线性做前端编码）的关系：那一条解决**输入编码**，
这一条解决**时域信号处理与读出**——两者共用 QD 光源与 MRR crossbar 平台，互为补充而非竞争。

---

## 一、研究背景与动机

### 1.1 电域带宽瓶颈

光域匹配滤波（啁啾光栅脉冲压缩）的输出脉宽 ≈ 光带宽倒数（~ps 级）。
要让电域 ADC 可读，信号必须被拉宽到 ns 级——这要求光栅色散量增大三个数量级，
器件长度和损耗在 TFLN 上难以为继。**电域带宽要求反过来给光器件施加了不合理的压力。**

### 1.2 本计划的切入点

换一个思路：**不让光器件去迁就电域时标，而是让光蓄水池在 ps 时标上处理，电域只读慢积分**。
光子蓄水池内部用 ps 级延迟抽头处理时序模式，输出节点由慢光电探测（τ_leak ~ ns）积分。
电路看到的空间模式（而非时间脉冲），电域带宽需求下降 2–3 个数量级。

### 1.3 为什么必须是 TFLN

光蓄水池要"聪明"，需要可调/可训练的延迟自由度。热光调谐太慢（µs），SiN/Si 缺乏高速电光效应。
**TFLN 的 Pockels 效应是唯一能在片上实现 ns 级、低功耗、可重构延迟调谐的机制**——
这是选择 TFLN 而非 SiN 的根本理由（SiN 的损耗更低，但只能做被动器件）。

---

## 二、仿真已验证的科学基础（本计划的支撑证据）

以下结论均来自 2026-09-20 的综合仿真（`comprehensive_simulation.py` 等，见附录）：

1. **器件层**：啁啾光栅把匹配啁啾脉冲压缩 22 倍（D=0.051 ps/nm，与几何预言偏差 8%）。
2. **时标分离**：在等能量等质心的严格任务上，直接慢积分准确率=随机（0.27），
   而"非线性蓄水池 + 漏电积分慢读出"= 1.00；线性蓄水池消融 = 0.40。
   **证明非线性蓄水池是慢读出成立的必要环节**，且答案存活时间由 τ_leak 决定。
3. **计算层**：LSM 蓄水池在 digits 上 94%、ModelNet40 点云上 40%（4× 随机）。
4. **创新点边界**：
   - 可调延迟的价值依赖延迟敏感的非线性/读出架构（时间平均下无增益）；
   - TFLN 色散的延迟/损耗品质因数 424 ps/dB，适合亚米级测距窗口；长距需 SiN 或异质混合。

---

## 三、核心科学问题

**SQ1（器件-架构耦合）**：TFLN 电光可调延迟元件的非线性/读出架构如何设计，
才能使蓄水池特征对延迟敏感且梯度可达？
——仿真已表明：时间平均特征对延迟天然不敏感，稀疏符合度特征梯度太稀疏。
可行路径是延迟敏感非线性 + surrogate gradient/连续速率训练。

**SQ2（物理约束）**：TFLN 色散器件的延迟-损耗-带宽三角如何突破亚米级测距窗口，
走向实用 LiDAR 量程？
——候选：spiral/级联光栅、SiN 延迟 + TFLN 调谐的异质混合、慢光增强。

**SQ3（系统闭环）**：QD 激光器 spike 生成、光栅压缩、可调蓄水池、慢读出如何级联为
一个端到端可训练、硬件感知的片上接收机？
——复用组里 QD-MLL PCU 与 HATF（硬件感知训练）框架。

---

## 四、研究内容与技术路线

### Objective 1：TFLN 可调延迟元件与延迟敏感蓄水池（SQ1）
- 仿真：分段电极啁啾光栅的电压→局部 λ_B→τ(λ) 映射；时间透镜的电带宽约束
- 算法：delay-sensitive 蓄水池架构 + surrogate gradient 训练，完成正向演示
- 器件：TFLN 相位调制区/微环的延迟调谐效率（ps/V）、串扰、功耗

### Objective 2：色散器件的延迟-损耗-带宽优化（SQ2）
- 定量：spiral/级联/异质混合三种路线的损耗-延迟-带宽 Pareto 前沿
- 目标：测距窗口从亚米级推向米级，同时保住电光可调

### Objective 3：端到端片上接收机演示（SQ3）
- 级联：QD 光源 → 光栅压缩 → 可调蓄水池 → 慢读出 → 线性分类
- 方法：硬件感知训练（HATF），把器件噪声/量化/漂移注入训练
- 验证：真实 LiDAR 点云（ModelNet40）与低 SNR 雷达检测任务

---

## 五、与课题组的契合度

| 组里资产 | 本计划的使用 | 依赖方向 |
|---|---|---|
| III-V/Si QD 锁模激光器、多波长光源 | 光 spike 生成器、蓄水池光源 | 需要 QD 芯片与驱动 |
| MRR crossbar 光子张量核心 | 蓄水池权重/读出 | 需要流片与工艺协同 |
| QD-MLL PCU + HATF 框架（Zhou 2026） | 硬件感知训练方法直接复用 | 已有基础 |
| 300 mm 硅光异质集成产线 | TFLN/SiN 器件晶圆级集成 | 需要流片名额 |
| Cheng Wang 组 TFLN 合作 | TFLN 光栅/调制器加工 | 借既有渠道 |

**历史连续性**：此前的"Laser-as-ReLU"（QD 激光器做前端编码）与本计划（光蓄水池做时域处理）
共用同一 QD 光源与 MRR 平台，研究主线（沿组的光源+计算平台做器件物理）一致，不是跳题。

---

## 六、预期成果与创新点

1. **器件创新**：TFLN 电光可调延迟元件（可重构 τ(λ) 映射）——SiN 给不了的自由度。
2. **架构创新**：延迟敏感蓄水池 + 慢读出的时标分离架构——把光器件从"迁就电域"中解放出来。
3. **方法创新**：蓄水池的硬件感知训练（HATF）+ delay learning 联合训练框架。
4. **系统创新**：片上自校准收发对的接收端（呼应 lab-note §10 的收发共用构想）。

---

## 七、风险与备选

| 风险 | 缓解 |
|---|---|
| delay learning 在所选架构上不收敛 | 已有三个对照实验定位可行路径；备选=固定池 + 强读出（仍可用） |
| TFLN 损耗限制测距窗口 | 异质混合（SiN 延迟 + TFLN 调谐）；亚米级场景先行验证 |
| 真实 QD spike 噪声/抖动退化性能 | M1 鲁棒性扫描已给容忍度；HATF 训练时注入 |
| 2D 仿真到 3D 器件的落差 | 先用 Cheng 组成熟 TFLN 工艺做被动光栅流片标定 |

---

## 附录：仿真证据文件

- `tfln-dispersion-lab/simulation_report_2026-09-20.docx` —— 综合仿真报告（本文全部数据的出处）
- `tfln-dispersion-lab/milestone-M1/M2/M3-*.ipynb` —— 三个里程碑的 Jupyter 总结
- `tfln-dispersion-lab/lab-note.ipynb` §16 —— 活笔记
- `lidar-pointnet/snn/` —— LSM/编码/分类/delay learning 代码
